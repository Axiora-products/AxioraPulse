name: AxioraPulse Master Pipeline

on:
  push:
    branches:
      - fix/workflow-paths-syntax
    paths:
      - '.github/workflows/**'

jobs:
  # 1. Quality Gate
  ci-quality-gate:
    uses: ./.github/workflows/ci.yml
    with:
      check_backend: true
      check_frontend: true
