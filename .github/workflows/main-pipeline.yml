name: AxioraPulse Master Pipeline

on:
  push:
    branches:
      - fix/workflow-paths-syntax
    paths:
      - '.github/workflows/**'

jobs:
  ci-quality-gate:
    uses: ./.github/workflows/ci.yml
