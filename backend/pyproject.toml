[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "B", "I"]
ignore = ["E501"] # Ignore line length errors (already handled by line-length)

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = "test_*.py"
